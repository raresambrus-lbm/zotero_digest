# Copy this file to zotero_digest.env and configure your settings

# Library settings
LIBRARY_TYPE=groups          # "users" or "groups"
LIBRARY_ID=1234567          # Your numeric library/group ID (optional if using GROUP_NAME)
GROUP_NAME=                 # Group name for auto-discovery (groups only)
LOCAL_TZ=America/Los_Angeles # Your timezone
SKIP_WEEKENDS=1             # Skip running on weekends (0=run always, 1=skip)
DEBUG_FILTER=0              # Show debug info (1=enabled, 0=disabled)

# Optional settings
WINDOW_DAYS=1                        # Days to look back (default: 1)
ZOTERO_COLLECTION=                   # 8-char collection key to filter (optional)
AUTO_ANNOTATION_THRESHOLD=1          # Minutes threshold for auto-annotations
PAPER_TYPES=                         # Custom paper types, comma-separated (optional)

# Note: ZOTERO_API_KEY and SLACK_WEBHOOK_URL should be set in your shell profile (.bashrc, .zshrc)
# export ZOTERO_API_KEY="your-api-key-here"
# export SLACK_WEBHOOK_URL="https://hooks.slack.com/services/YOUR/WEBHOOK/URL"